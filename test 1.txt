// ==UserScript==
// @name         Test 1
// @namespace    http://tampermonkey.net/
// @version      6.0
// @description  Автоматически выбирает все правильные ответы и нажимает "Ответить"
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/119
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "Какой тип коммутации использует IP-телефония для функционирования?": [
        "коммутацию пакетов"
    ],
    "Какой тип коммутации используют ТфОП для функционирования?": [
        "коммутацию каналов"
    ],
    "Во что преобразуется наш разговор для осуществления IP-передачи ?": [
        "В пакеты данных"
    ],
    "В случае установки модемного соединения со скоростью 56Кб, при передаче речи в режиме VoIP какое теоретическое количество одновременных разговоров можно провести, если использовать кодек G729?": [
        "7 и более"
    ],
    "Вы заключили контракт с интернет провайдером на предоставление Вам Интернета с гарантированным каналом 256 Upload /128 Download. При передаче речи в режиме VoIP какое теоретическое количество одновременных разговоров Вы можете провести? … и более.": [
        "16"
    ],
    "Если абонент при разговоре молчит или делает паузы IP-канал ... .": [
        "не заполняется"
    ],
    "Имея в наличии интернет соединение мы можем одновременно посылать или принимать": [
        "голосовые данные",
        "обычные данные"
    ],
    "IP-телефония передается по тем-же каналам что и": [
        "данные"
    ],
    "Интернет-телефония более полно использует емкость телефонных линий за счет": [
        "прогрессивной технологии сжатия"
    ],
    "Архитектура VoIP является": [
        "открытой"
    ],
    "Одной из причин способствующей быстрому распространению VoIP является использование ... архитектуры.": [
        "открытой"
    ],
    "При окончании телефонного разговора разъединение с любой стороны передается противоположной стороне по протоколу": [
        "сигнализации"
    ],
    "Общие протоколы IP-телефонии (укажите наиболее полный ответ).": [
        "H.323, SIP, MGCP"
    ],
    "Самым используемым на сегодняшний день протоколом IP-телефонии является протокол": [
        "SIP"
    ],
    "Протоколами IP-телефонии являются": [
        "SIP",
        "H.323",
        "MGCP"
    ],
    "При передаче, голосовой сигнал:": [
        "оцифровывается, сжимается и упаковывается в IP-пакеты"
    ],
    "Оцифровка голосового сигнала происходит внутри": [
        "шлюза"
    ],
    "В оцифровке голосового сообщения участвуют": [
        "аппаратные и программные средства"
    ],
    "Качество упаковки голосового сообщения (используемого кодека)...": [
        "обратно пропорционально качеству передаваемого голосового сообщения"
    ],
    "Сжатие голосового сообщения происходит с помощью": [
        "кодека"
    ],
    "Укажите корректную иерархию протоколов Интернета при осуществлении запаковки IP-звонка": [
        "RTP/UDP/IP"
    ],
    "При осуществлении телефонного звонка посредством IP-телефона с голосовым сообщением происходит": [
        "оба преобразования и аналого-цифровое, и цифро-аналоговое"
    ],
    "АЦП - это": [
        "аналого-цифровое преобразование"
    ],
    "На принимающей стороне используется... преобразование для восстановления голосового сообщения": [
        "цифрово-аналоговое преобразование"
    ],
    "IP-телефоном можно назвать": [
        "компьютер с микрофоном и колонками и запущенным программным обеспечением IP-телефонии",
        "специализированный терминал имеющий вид обычного телефона с записанным в память программным обеспечением IP-телефонии",
        "связку стандартный телефон подключенный к IP-шлюзу"
    ],
    "Для осуществления телефонного звонка посредством IP-телефонии можно воспользоваться следующим оборудованием:": [
        "специализированный терминал, имеющий вид обычного телефона с записанным в память программным обеспечением IP-телефонии",
        "компьютер с микрофоном и колонками и запущенным программным обеспечением IP-телефонии"
    ],
    "В сетях с маршрутизацией пакетов IP для передачи данных всегда предусматриваются механизмы": [
        "повторной передачи пакетов в случае их потери"
    ],
    "Используя IP-телефонию вы можете осуществлять телефонные звонки:": [
        "компьютер-компьютер",
        "телефон-компьютер",
        "телефон-телефон"
    ],
    "Для передачи речи и видеоинформации в IP-телефонии используется механизм": [
        "негарантированной доставки информации"
    ],
    "Инструментом управления адресации, направлением и способом вызова, а также типом установления соединения являются": [
        "телефонные системы сигнализации"
    ],
    "Экономия при международных звонках при использовании IP-телефонии возникает за счет:": [
        "использования частной IP-сети либо сети интернет",
        "возможности не использовать сети общего пользования"
    ],
    "Поставщики услуг IP-телефонии, при предоставлении возможности осуществления телефонных звонков, для кодирования голосового сигнала используют:": [
        "шлюзы"
    ],
    "Конфликт интересов между поставщиками традиционной телефонии и IP-телефонии возникает в случае осуществления звонков типа": [
        "телефон-телефон"
    ]
};

    let currentAnswerIndex = 0;
    let currentQuestionAnswers = null;
    let selectedAnswersCount = 0;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска всех подходящих вопросов
    function findAllMatchingQuestions(questionText) {
        const normalizedQuestion = normalizeText(questionText);
        let matchingQuestions = [];

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                matchingQuestions.push({ question, answers, type: 'exact', priority: 1 });
                continue;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                matchingQuestions.push({ question, answers, type: 'contains', priority: 2 });
                continue;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                matchingQuestions.push({ question, answers, type: 'partial', priority: 3, percentage: matchPercentage });
            }
        }

        // Сортируем по приоритету
        matchingQuestions.sort((a, b) => a.priority - b.priority);

        return matchingQuestions;
    }

    // Функция для выбора одного ответа по очереди
    function selectNextAnswer() {
        console.log('🎯 ВЫБОР СЛЕДУЮЩЕГО ОТВЕТА');

        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            console.log('❌ Элемент вопроса не найден');
            return;
        }

        const questionText = questionElement.textContent.trim();

        // Если это новый вопрос, найдем все правильные ответы
        if (!currentQuestionAnswers) {
            console.log(`📝 НОВЫЙ ВОПРОС: "${questionText}"`);

            const matchingQuestions = findAllMatchingQuestions(questionText);

            if (matchingQuestions.length === 0) {
                console.log('❌ Подходящие вопросы не найдены');
                return;
            }

            // Берем первый подходящий вопрос
            currentQuestionAnswers = matchingQuestions[0].answers;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;

            console.log(`✅ ПРАВИЛЬНЫЕ ОТВЕТЫ (${currentQuestionAnswers.length}):`, currentQuestionAnswers);
        }

        // Если все ответы уже выбраны
        if (currentAnswerIndex >= currentQuestionAnswers.length) {
            console.log('✅ ВСЕ ОТВЕТЫ ВЫБРАНЫ! Можете нажать "Ответить" вручную.');
            // Сбрасываем для следующего вопроса
            currentQuestionAnswers = null;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;
            return;
        }

        const targetAnswer = currentQuestionAnswers[currentAnswerIndex];
        console.log(`🔍 ИЩЕМ ОТВЕТ ${currentAnswerIndex + 1}/${currentQuestionAnswers.length}: "${targetAnswer}"`);

        let answerOptions = document.querySelectorAll('.wrapper label.option');
        if (answerOptions.length === 0) {
            answerOptions = document.querySelectorAll('label.option');
        }

        if (answerOptions.length === 0) {
            console.log('❌ Варианты ответов не найдены');
            return;
        }

        let found = false;

        answerOptions.forEach((option, index) => {
            if (found) return;

            const rightSpan = option.querySelector('.right');
            const optionText = rightSpan ? rightSpan.textContent : option.textContent;

            console.log(`   Проверяем вариант ${index + 1}: "${optionText.trim()}"`);

            if (textsMatch(optionText, targetAnswer)) {
                let input = option.querySelector('input[type="radio"], input[type="checkbox"]');
                if (input && !input.checked) {
                    input.checked = true;
                    input.dispatchEvent(new Event('change', { bubbles: true }));
                    input.dispatchEvent(new Event('click', { bubbles: true }));
                    console.log(`✅ ВЫБРАН вариант ${index + 1}: "${optionText.trim()}"`);
                    found = true;
                    selectedAnswersCount++;
                    currentAnswerIndex++;
                } else if (input && input.checked) {
                    console.log(`⚠️ Вариант ${index + 1} уже выбран, переходим к следующему`);
                    found = true;
                    currentAnswerIndex++;
                }
            }
        });

        if (!found) {
            console.log(`❌ Ответ "${targetAnswer}" не найден, переходим к следующему`);
            currentAnswerIndex++;

            // Если не нашли текущий ответ, пробуем следующий
            if (currentAnswerIndex < currentQuestionAnswers.length) {
                setTimeout(() => selectNextAnswer(), 500);
            }
        }

        console.log(`📊 ПРОГРЕСС: ${selectedAnswersCount}/${currentQuestionAnswers.length} ответов выбрано`);
    }

    // Обработчик клавиш
    function handleKeyPress(event) {
        if (event.key === '2' || event.keyCode === 50) {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
                selectNextAnswer();
            }
        }

        // Клавиша R для сброса
        if (event.key === 'r' || event.key === 'R') {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                currentQuestionAnswers = null;
                currentAnswerIndex = 0;
                selectedAnswersCount = 0;
                console.log('🔄 Состояние сброшено');
            }
        }
    }

    // Инициализация
    function init() {
        document.addEventListener('keydown', handleKeyPress);
        document.addEventListener('keypress', handleKeyPress);
        console.log('🚀 СКРИПТ INTUIT AUTO COMPLETE ЗАПУЩЕН!');
        console.log('📋 Нажмите "2" для выбора следующего правильного ответа');
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();