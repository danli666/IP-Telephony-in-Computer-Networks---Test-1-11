// ==UserScript==
// @name         Test 8
// @namespace    http://tampermonkey.net/
// @version      6.0
// @description  Автоматически выбирает все правильные ответы и нажимает "Ответить"
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/126
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "Основными элементами безопасности являются": [
        "аутентификация",
        "целостность",
        "активная проверка"
    ],
    "Под целостностью информации понимают": [
        "способность вычислительной техники или автоматизированной системы обеспечивать неизменность информации в условиях случайного и (или) преднамеренного искажения"
    ],
    "Отметьте основные типы угроз, представляющих опасность в сетях IP-телефонии": [
        "DoS-атаки",
        "манипулирование данными",
        "прослушивание"
    ],
    "Атаки типа DOS обнаруживаются на этапе": [
        "активной проверки"
    ],
    "Криптография является составляющей механизмов": [
        "конфиденциальности",
        "аутентификации",
        "целостности"
    ],
    "Механизм конфиденциальности": [
        "создает ситуацию когда данные понятны только отправителю и получателю"
    ],
    "Процесс аутентификации подтверждает": [
        "отправителя",
        "получателя"
    ],
    "Технология шифрования симметричного алгоритма требует": [
        "доступ к одному и тому же ключу отправителя и получателя"
    ],
    "Укажите требования к криптосистемам": [
        "невозможность раскрытия или осмысленной модификации информации на основе анализа ее структуры",
        "минимальный объем используемой ключевой информации",
        "минимальная сложность реализации"
    ],
    "Алгоритм шифрования DES использует следующий криптографический метод": [
        "симметричное шифрование"
    ],
    "Укажите корректные утверждения": [
        "системы с асимметричным шифрованием характеризуются наличием у каждого абонента двух ключей: открытого и закрытого (секретного)",
        "системы с асимметричным шифрованием не требуют секретной доставки ключа",
        "криптосистемах с открытым ключом отсутствует квадратичная зависимость числа ключей от числа пользователей"
    ],
    "Укажите основные недостатки систем открытого шифрования": [
        "надежность канала передачи ключа второму участнику",
        "зависимость числа ключей от числа абонентов является квадратичной"
    ],
    "RSA использует метод": [
        "асимметричного шифрование"
    ],
    "RSA закрытый ключ можно вычислить на основании": [
        "открытого ключа путем полного перебора"
    ],
    "Отметьте верные утверждения": [
        "по открытому ключу очень сложно вычислить парный ему личный ключ",
        "увеличение мощности процессоров позволит применить более длинные RSA ключи",
        "увеличение мощности процессоров снизит стойкость RSA ключей"
    ],
    "Электронная цифровая подпись (ЭЦП) применяется для": [
        "проверки источника данных",
        "проверки целостности данных"
    ],
    "Какой алгоритм позволяет двум сторонам создать общий для них секретный ключ, известный только им двоим, несмотря на осуществление связи по незащищенному каналу": [
        "алгоритм Диффи-Хеллмана"
    ],
    "Укажите алгоритмы ХЭШ-функций": [
        "MD4",
        "MD5",
        "SHA"
    ],
    "Цифровой сертификат это": [
        "цифровой документ, который подтверждает соответствие открытого ключа и информации о владельце ключа"
    ],
    "Terminal Access Controller Access Control System - это": [
        "протокол управления доступом, основанный на стандартах User Datagram Protocol"
    ],
    "Какие протоколы использует механизм аутентификации по протоколу Point-to-Point Protocol (PPP)?": [
        "CHAP",
        "PAP",
        "EAP"
    ],
    "аутентификация это": [
        "процесс идентификации"
    ],
    "устройство защиты голосовой инфраструктуры от возможных несанкционированных воздействий должно быть способно анализировать": [
        "заголовок и тело пакета данных"
    ],
    "Отметьте возможные методы аутентификации": [
        "использование протокола S/Key",
        "использование паролей",
        "отпечатки пальцев",
        "token password authentication"
    ],
    "Аутентификация для протокола PPP": [
        "опциональна"
    ],
    "Какой протокол используют компании для создания баланса взаиморасчетов": [
        "OSP"
    ],
    "VPN предусматривает организацию безопасности передаваемых данных по общедоступным сетям путем": [
        "шифрования"
    ],
    "Авторизация - это": [
        "процесс определения действий и прав, которые позволены аутентифицированному пользователю"
    ],
    "В рамках протокола VPN проверка работоспособности абонента в конкретный момент времени обеспечивается протоколом": [
        "IKE"
    ],
    "Для формирования туннелей VPN используются протоколы": [
        "PPTP",
        "IPSec",
        "L2TP"
    ],
    "Протокол RADIUS основан на технологии": [
        "клиент-сервер"
    ],
    "Защита конфиденциальности разговоров IP телефонии с использованием алгоритмов и протоколов (DES, 3DES, AES, IPSec и т. п.) возможна при звонках типа": [
        "IP - IP"
    ],
    "Основу любой инфраструктуры IP-телефонии составляет": [
        "сетевое оборудование"
    ]
};



    let currentAnswerIndex = 0;
    let currentQuestionAnswers = null;
    let selectedAnswersCount = 0;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска всех подходящих вопросов
    function findAllMatchingQuestions(questionText) {
        const normalizedQuestion = normalizeText(questionText);
        let matchingQuestions = [];

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                matchingQuestions.push({ question, answers, type: 'exact', priority: 1 });
                continue;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                matchingQuestions.push({ question, answers, type: 'contains', priority: 2 });
                continue;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                matchingQuestions.push({ question, answers, type: 'partial', priority: 3, percentage: matchPercentage });
            }
        }

        // Сортируем по приоритету
        matchingQuestions.sort((a, b) => a.priority - b.priority);

        return matchingQuestions;
    }

    // Функция для выбора одного ответа по очереди
    function selectNextAnswer() {
        console.log('🎯 ВЫБОР СЛЕДУЮЩЕГО ОТВЕТА');

        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            console.log('❌ Элемент вопроса не найден');
            return;
        }

        const questionText = questionElement.textContent.trim();

        // Если это новый вопрос, найдем все правильные ответы
        if (!currentQuestionAnswers) {
            console.log(`📝 НОВЫЙ ВОПРОС: "${questionText}"`);

            const matchingQuestions = findAllMatchingQuestions(questionText);

            if (matchingQuestions.length === 0) {
                console.log('❌ Подходящие вопросы не найдены');
                return;
            }

            // Берем первый подходящий вопрос
            currentQuestionAnswers = matchingQuestions[0].answers;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;

            console.log(`✅ ПРАВИЛЬНЫЕ ОТВЕТЫ (${currentQuestionAnswers.length}):`, currentQuestionAnswers);
        }

        // Если все ответы уже выбраны
        if (currentAnswerIndex >= currentQuestionAnswers.length) {
            console.log('✅ ВСЕ ОТВЕТЫ ВЫБРАНЫ! Можете нажать "Ответить" вручную.');
            // Сбрасываем для следующего вопроса
            currentQuestionAnswers = null;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;
            return;
        }

        const targetAnswer = currentQuestionAnswers[currentAnswerIndex];
        console.log(`🔍 ИЩЕМ ОТВЕТ ${currentAnswerIndex + 1}/${currentQuestionAnswers.length}: "${targetAnswer}"`);

        let answerOptions = document.querySelectorAll('.wrapper label.option');
        if (answerOptions.length === 0) {
            answerOptions = document.querySelectorAll('label.option');
        }

        if (answerOptions.length === 0) {
            console.log('❌ Варианты ответов не найдены');
            return;
        }

        let found = false;

        answerOptions.forEach((option, index) => {
            if (found) return;

            const rightSpan = option.querySelector('.right');
            const optionText = rightSpan ? rightSpan.textContent : option.textContent;

            console.log(`   Проверяем вариант ${index + 1}: "${optionText.trim()}"`);

            if (textsMatch(optionText, targetAnswer)) {
                let input = option.querySelector('input[type="radio"], input[type="checkbox"]');
                if (input && !input.checked) {
                    input.checked = true;
                    input.dispatchEvent(new Event('change', { bubbles: true }));
                    input.dispatchEvent(new Event('click', { bubbles: true }));
                    console.log(`✅ ВЫБРАН вариант ${index + 1}: "${optionText.trim()}"`);
                    found = true;
                    selectedAnswersCount++;
                    currentAnswerIndex++;
                } else if (input && input.checked) {
                    console.log(`⚠️ Вариант ${index + 1} уже выбран, переходим к следующему`);
                    found = true;
                    currentAnswerIndex++;
                }
            }
        });

        if (!found) {
            console.log(`❌ Ответ "${targetAnswer}" не найден, переходим к следующему`);
            currentAnswerIndex++;

            // Если не нашли текущий ответ, пробуем следующий
            if (currentAnswerIndex < currentQuestionAnswers.length) {
                setTimeout(() => selectNextAnswer(), 500);
            }
        }

        console.log(`📊 ПРОГРЕСС: ${selectedAnswersCount}/${currentQuestionAnswers.length} ответов выбрано`);
    }

    // Обработчик клавиш
    function handleKeyPress(event) {
        if (event.key === '2' || event.keyCode === 50) {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
                selectNextAnswer();
            }
        }

        // Клавиша R для сброса
        if (event.key === 'r' || event.key === 'R') {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                currentQuestionAnswers = null;
                currentAnswerIndex = 0;
                selectedAnswersCount = 0;
                console.log('🔄 Состояние сброшено');
            }
        }
    }

    // Инициализация
    function init() {
        document.addEventListener('keydown', handleKeyPress);
        document.addEventListener('keypress', handleKeyPress);
        console.log('🚀 СКРИПТ INTUIT AUTO COMPLETE ЗАПУЩЕН!');
        console.log('📋 Нажмите "2" для выбора следующего правильного ответа');
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();