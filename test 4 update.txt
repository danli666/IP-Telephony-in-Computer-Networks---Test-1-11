// ==UserScript==
// @name         Test 4 - Auto Select on Hover
// @namespace    http://tampermonkey.net/
// @version      6.3
// @description  Автоматически выбирает правильные ответы при наведении курсора
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/122
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
        "Оборудование стандарта H.323 может передавать": [
            "данные, видео и аудио информацию"
        ],
        "Сеть, через которую осуществляется связь между терминалами H.323 может быть построена на базе": [
            "одноуровневой линейной топологии",
            "множества сегментов со сложной топологией",
            "топологии сети типа звезда"
        ],
        "H.323 предусматривает рекомендации": [
            "управление полосой пропускания",
            "стандарты для кодеков",
            "поддержку многоточечных конференций",
            "поддержку групповой адресации"
        ],
        "Стандарт Н.323 позволяет использовать": [
            "разные платформы, с разными операционными системами"
        ],
        "H.323 поддерживает многоадресную передачу. При многоадресной передаче": [
            "один пакет информации отправляется всем необходимым адресатам без дублирования"
        ],
        "Рекомендации H.323 предусматривают": [
            "ограничение числа одновременных соединений",
            "управление полосой пропускания",
            "ограничение суммарной полосы пропускания для всех приложений H.323"
        ],
        "Рекомендации H.323 устанавливают следующие основные компоненты VoIP- соединения": [
            "терминал, контроллер зоны, шлюз, MCU"
        ],
        "Какие протоколы используются терминалами для управления аутентификацией": [
            "H.225",
            "RAS"
        ],
        "Какие протоколы базово поддерживаются H.323 терминалами": [
            "H.245",
            "H.225",
            "RTP/RTCP"
        ],
        "Терминал H.323 в статическом режиме": [
            "адрес контроллера прописан в памяти терминала"
        ],
        "В состав дополнительных услуг стека протоколов H.323 включены следующие услуги": [
            "переадресация вызова",
            "уведомление о поступившем вызове в состоянии разговора",
            "парковка вызова (park) и ответ на вызов (pick up)",
            "служба идентификации имен"
        ],
        "H.225 сигнализация используется для": [
            "установления соединения между двумя H.323 оконечными точками"
        ],
        "RAS-канал используется для реализации таких механизмов управления как": [
            "контроль аутентификации",
            "управление шириной полосы пропускания"
        ],
        "Если в H.323-сети нет контроллера зоны, то": [
            "конечные точки обмениваются сигналами вызовов непосредственно друг с другом"
        ],
        "Для осуществления звонка с IP-телефона на номер ТфОП обязательно присутствие": [
            "шлюза"
        ],
        "Шлюзы H.323 применяются в IP-телефонии для": [
            "сопряжения IP-сетей и аналоговых коммутируемых телефонных сетей",
            "сопряжения IP-сетей и цифровых коммутируемых телефонных сетей",
            "сопряжения IP-сетей между собой"
        ],
        "При отсутствии в сети шлюза": [
            "обязательно нужно реализовать функцию преобразования номера ТфОП в транспортный адрес IP-сети"
        ],
        "Многоточечную конференцию можно организовать, используя методы конфигурации": [
            "централизованная",
            "гибридная",
            "смешанная"
        ],
        "Централизованная многоточечная конференция требует наличия": [
            "MCU"
        ],
        "Multipoint Processors (MP) отвечают за": [
            "микширование",
            "обработку"
        ],
        "Gatekeeper является": [
            "рекомендуемым"
        ],
        "Маршрутизация сигналов вызова является": [
            "факультативной возможностью контроллера зоны",
            "возможностью реализуемой в шлюзе или в контроллере зоны"
        ],
        "Маршрутизация обращений через контроллер зоны": [
            "обеспечивает лучшую эффективность работы сети"
        ],
        "Факультативные функции контроллера зоны": [
            "авторизация вызова",
            "управление вызовами"
        ],
        "Обязательные функции контроллера зоны": [
            "трансляция адреса",
            "управление регистрацией",
            "управление полосой пропускания"
        ],
        "Услуги, предлагаемые контроллером зоны определяются в": [
            "RAS"
        ],
        "Укажите корректную последовательность процедуры соединения по H.323": [
            "1 - Setup / 2 - Alert / 3 - Connecting / 4 - TCS / 5 - TCSA и т.д."
        ],
        "TerminalCapabilitySetReject": [
            "таким сообщением отклоняются установки возможностей терминала"
        ],
        "Отметьте верные утверждения": [
            "H.323 намного сложнее реализации SIP",
            "H.323 требует и TCP, и UDP во время установки соединения",
            "SIP требует около 4 пакетов для установки соединения"
        ],
        "Протокол RSVP": [
            "позволяет резервировать"
        ],
        "Достоинствами стандарта H.323 является": [
            "технология H.323 допускает возможность взаимодействия оборудования от разных производителей",
            "технология H.323 может применяться для организации междугородных и международных телефонных соединений с целью снижения их стоимости",
            "технология H.323 не зависит от аппаратного обеспечения и операционной системы",
            "в корпоративной интрасети H.323 может обеспечивать более надежные соединения и уменьшать проблемы поддержки"
        ]
    };

    let currentQuestionText = null;
    let currentCorrectAnswers = null;
    let hoverEnabled = true;
    let hoverTimer = null;
    let currentHoverElement = null;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска правильных ответов для текущего вопроса
    function findCorrectAnswersForQuestion(questionText) {
        const normalizedQuestion = normalizeText(questionText);

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                return answers;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                return answers;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                return answers;
            }
        }

        return null;
    }

    // Функция для получения текущего вопроса
    function getCurrentQuestion() {
        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            return null;
        }

        return questionElement.textContent.trim();
    }

    // Функция для обновления кэша правильных ответов
    function updateCorrectAnswersCache() {
        const questionText = getCurrentQuestion();

        if (!questionText) {
            currentQuestionText = null;
            currentCorrectAnswers = null;
            return;
        }

        // Если вопрос изменился
        if (questionText !== currentQuestionText) {
            console.log(`📝 Текущий вопрос: "${questionText}"`);
            currentQuestionText = questionText;
            currentCorrectAnswers = findCorrectAnswersForQuestion(questionText);

            if (currentCorrectAnswers) {
                console.log(`✅ Найдены правильные ответы (${currentCorrectAnswers.length}):`, currentCorrectAnswers);
            } else {
                console.log('❌ Правильные ответы не найдены');
            }
        }
    }

    // Функция для проверки, является ли вариант ответа правильным
    function isOptionCorrect(optionElement) {
        if (!currentCorrectAnswers || !hoverEnabled) return false;

        const rightSpan = optionElement.querySelector('.right');
        const optionText = rightSpan ? rightSpan.textContent : optionElement.textContent;

        return currentCorrectAnswers.some(correctAnswer =>
            textsMatch(optionText, correctAnswer)
        );
    }

    // Функция для выбора варианта ответа
    function selectOption(optionElement) {
        if (!hoverEnabled) return;

        const input = optionElement.querySelector('input[type="radio"], input[type="checkbox"]');
        if (!input) return;

        if (!input.checked) {
            input.checked = true;
            input.dispatchEvent(new Event('change', { bubbles: true }));
            input.dispatchEvent(new Event('click', { bubbles: true }));

            console.log(`✅ Автоматически выбран: "${optionElement.textContent.trim()}"`);
        }
    }

    // Обработчик наведения мыши
    function handleMouseOver(event) {
        updateCorrectAnswersCache();

        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Проверяем, правильный ли это ответ
        if (isOptionCorrect(target)) {
            currentHoverElement = target;

            // Запускаем таймер с задержкой 500ms
            hoverTimer = setTimeout(() => {
                if (currentHoverElement === target) {
                    selectOption(target);
                    currentHoverElement = null;
                }
            }, 500); // 0.5 секунды задержки
        }
    }

    // Обработчик ухода мыши с элемента
    function handleMouseOut(event) {
        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Если ушли с элемента, который был наведен, отменяем таймер
        if (target === currentHoverElement) {
            clearTimeout(hoverTimer);
            hoverTimer = null;
            currentHoverElement = null;
        }
    }

    // Обработчик клавиш для отладки
    function handleKeyPress(event) {
        // Ctrl+Shift+D для вывода отладочной информации
        if (event.ctrlKey && event.shiftKey && event.key === 'D') {
            console.log('=== ОТЛАДОЧНАЯ ИНФОРМАЦИЯ ===');
            console.log('Текущий вопрос:', currentQuestionText);
            console.log('Правильные ответы:', currentCorrectAnswers);
            console.log('Автовыбор включен:', hoverEnabled);
        }

        // Ctrl+Shift+T для переключения режима
        if (event.ctrlKey && event.shiftKey && event.key === 'T') {
            hoverEnabled = !hoverEnabled;
            console.log(hoverEnabled ? '✅ Автовыбор включен' : '❌ Автовыбор выключен');
        }
    }

    // Инициализация
    function init() {
        // Добавляем обработчик наведения мыши
        document.addEventListener('mouseover', handleMouseOver);
        document.addEventListener('mouseout', handleMouseOut);

        // Добавляем обработчик клавиш для отладки
        document.addEventListener('keydown', handleKeyPress);

        // Обновляем кэш при клике (для перехода к следующему вопросу)
        document.addEventListener('click', updateCorrectAnswersCache);

        console.log('🚀 СКРИПТ АВТОВЫБОРА ЗАПУЩЕН!');
        console.log('🎯 Наведите курсор на правильный ответ для автоматического выбора');
        console.log('⏱️  Автовыбор произойдет через 0.5 секунды после наведения');
        console.log('🔄 Нажмите Ctrl+Shift+T для переключения режима автовыбора');
        console.log('📊 Нажмите Ctrl+Shift+D для отладочной информации');
    }

    // Запуск при загрузке страницы
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();
