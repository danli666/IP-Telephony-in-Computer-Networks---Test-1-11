// ==UserScript==
// @name         Test 9
// @namespace    http://tampermonkey.net/
// @version      6.0
// @description  Автоматически выбирает все правильные ответы и нажимает "Ответить"
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/127
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "Сети IP-телефонии поддерживают следующие типы мобильности": [
        "режим виртуальной домашней сети",
        "мобильность пользователя",
        "мобильность обслуживания"
    ],
    "Как называются программные средства, повторяющие функциональность телефонов": [
        "softphone"
    ],
    "Укажите корректные идентификаторы терминала": [
        "транспортный адрес",
        "идентификатор оборудования"
    ],
    "Укажите тип мобильности использующий возможность пользователя соединяться с сетью IP телефонии, используя для соединения различные типы терминалов": [
        "мобильность пользователя"
    ],
    "Абонентский идентификатор является": [
        "идентификатором пользователя"
    ],
    "Укажите корректные идентификаторы приложения": [
        "адреса приложений транспортного уровня",
        "идентификатор конечной точки",
        "идентификатор точки доступа"
    ],
    "Мобильность обслуживания определяется как": [
        "вне зависимости от местонахождения и типа терминала разрешает пользование услугой"
    ],
    "Serving Area - это": [
        "зона обслуживания, которая может включать несколько подсетей, обслуживаемых одним АРоА"
    ],
    "Перечислите элементы сети IP-телефонии участвующие при реализации функций мобильности": [
        "HE",
        "APoA",
        "NPoA"
    ],
    "Абоненты сотовых сетей могут осуществлять IP-звонки посредством использования": [
        "GPRS",
        "EDGE"
    ],
    "Network Point of Attachment (NPoA) - это": [
        "точка подключения сети"
    ],
    "Мобильность между подсетями в пределах одной зоны обслуживания осуществляется на уровне": [
        "NPoA"
    ],
    "Терминал может быть идентифицирован как": [
        "оборудование",
        "приложение"
    ],
    "IP Application Point of Attachment (APoA) - это": [
        "точка подключения IP-приложения"
    ],
    "Мобильность между зонами обслуживания осуществляется на уровне": [
        "APoA"
    ],
    "Для определения пользователя используются идентификаторы": [
        "абонента",
        "роуминга",
        "пользователя"
    ],
    "Subnet - это": [
        "подсеть, обслуживаемая одним NPoA"
    ],
    "Мобильность между подсетями, находящимися в разных зонах обслуживания осуществляется на уровне": [
        "NPoA"
    ],
    "В функции АРоА входит": [
        "обеспечение соединения мобильного абонента с сетью на прикладном уровне"
    ],
    "Незаметно для пользователя переключаться между сетями в зависимости от удаленности пользователя и качества сигнала позволяет использование": [
        "специальных терминалов dual-mode handset (DMH)"
    ],
    "Под мобильностью SIP понимают": [
        "независимость от производителя оборудования"
    ],
    "Network Point of Attachment (NPoA) обеспечивает соединение между": [
        "IP-сетями"
    ],
    "Нomе Entity (HE) - это": [
        "домашний компонент, который управляет установлением соединения с вызываемым абонентом"
    ],
    "Мобильность между подсетями и между зонами обслуживания осуществляется на уровне": [
        "NPoA",
        "APoA"
    ],
    "В сетях IP-телефонии возможны следующие сценарии мобильности:": [
        "между подсетями и зонами обслуживания одновременно",
        "между зонами обслуживания",
        "между подсетями, находящимися в разных зонах обслуживания"
    ],
    "Режим виртуальной домашней сети позволяет": [
        "воспользоваться пакетом услуг, вне зависимости от местонахождения и типа терминала"
    ],
    "Мобильность терминала позволяет": [
        "менять физическое местонахождение, сохраняя способность соединения с сетью"
    ],
    "Мобильность пользователя в H.323-протоколе реализована с помощью": [
        "уникального персонального идентификатора"
    ],
    "Под roaming-ом понимают": [
        "изменение физического местонахождения терминала за пределами сеанса связи с сетью"
    ],
    "Под handover-ом понимают": [
        "изменение физического местонахождения терминала в пределах сеанса связи с сетью с потерей или без потери передаваемых данных"
    ],
    "Использование какого протокола позволит вам перемещаться по городу в зоне действия WiFI предоставляя возможность звонить вам на один и тот же номер ?": [
        "H.323",
        "SIP"
    ],
    "Использовать по выбору сотовый канал или Wi-Fi позволяют терминалы": [
        "DMH"
    ],
    "Отметьте виды мобильности терминалов": [
        "roaming",
        "handover"
    ]
};

 

    let currentAnswerIndex = 0;
    let currentQuestionAnswers = null;
    let selectedAnswersCount = 0;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска всех подходящих вопросов
    function findAllMatchingQuestions(questionText) {
        const normalizedQuestion = normalizeText(questionText);
        let matchingQuestions = [];

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                matchingQuestions.push({ question, answers, type: 'exact', priority: 1 });
                continue;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                matchingQuestions.push({ question, answers, type: 'contains', priority: 2 });
                continue;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                matchingQuestions.push({ question, answers, type: 'partial', priority: 3, percentage: matchPercentage });
            }
        }

        // Сортируем по приоритету
        matchingQuestions.sort((a, b) => a.priority - b.priority);

        return matchingQuestions;
    }

    // Функция для выбора одного ответа по очереди
    function selectNextAnswer() {
        console.log('🎯 ВЫБОР СЛЕДУЮЩЕГО ОТВЕТА');

        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            console.log('❌ Элемент вопроса не найден');
            return;
        }

        const questionText = questionElement.textContent.trim();

        // Если это новый вопрос, найдем все правильные ответы
        if (!currentQuestionAnswers) {
            console.log(`📝 НОВЫЙ ВОПРОС: "${questionText}"`);

            const matchingQuestions = findAllMatchingQuestions(questionText);

            if (matchingQuestions.length === 0) {
                console.log('❌ Подходящие вопросы не найдены');
                return;
            }

            // Берем первый подходящий вопрос
            currentQuestionAnswers = matchingQuestions[0].answers;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;

            console.log(`✅ ПРАВИЛЬНЫЕ ОТВЕТЫ (${currentQuestionAnswers.length}):`, currentQuestionAnswers);
        }

        // Если все ответы уже выбраны
        if (currentAnswerIndex >= currentQuestionAnswers.length) {
            console.log('✅ ВСЕ ОТВЕТЫ ВЫБРАНЫ! Можете нажать "Ответить" вручную.');
            // Сбрасываем для следующего вопроса
            currentQuestionAnswers = null;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;
            return;
        }

        const targetAnswer = currentQuestionAnswers[currentAnswerIndex];
        console.log(`🔍 ИЩЕМ ОТВЕТ ${currentAnswerIndex + 1}/${currentQuestionAnswers.length}: "${targetAnswer}"`);

        let answerOptions = document.querySelectorAll('.wrapper label.option');
        if (answerOptions.length === 0) {
            answerOptions = document.querySelectorAll('label.option');
        }

        if (answerOptions.length === 0) {
            console.log('❌ Варианты ответов не найдены');
            return;
        }

        let found = false;

        answerOptions.forEach((option, index) => {
            if (found) return;

            const rightSpan = option.querySelector('.right');
            const optionText = rightSpan ? rightSpan.textContent : option.textContent;

            console.log(`   Проверяем вариант ${index + 1}: "${optionText.trim()}"`);

            if (textsMatch(optionText, targetAnswer)) {
                let input = option.querySelector('input[type="radio"], input[type="checkbox"]');
                if (input && !input.checked) {
                    input.checked = true;
                    input.dispatchEvent(new Event('change', { bubbles: true }));
                    input.dispatchEvent(new Event('click', { bubbles: true }));
                    console.log(`✅ ВЫБРАН вариант ${index + 1}: "${optionText.trim()}"`);
                    found = true;
                    selectedAnswersCount++;
                    currentAnswerIndex++;
                } else if (input && input.checked) {
                    console.log(`⚠️ Вариант ${index + 1} уже выбран, переходим к следующему`);
                    found = true;
                    currentAnswerIndex++;
                }
            }
        });

        if (!found) {
            console.log(`❌ Ответ "${targetAnswer}" не найден, переходим к следующему`);
            currentAnswerIndex++;

            // Если не нашли текущий ответ, пробуем следующий
            if (currentAnswerIndex < currentQuestionAnswers.length) {
                setTimeout(() => selectNextAnswer(), 500);
            }
        }

        console.log(`📊 ПРОГРЕСС: ${selectedAnswersCount}/${currentQuestionAnswers.length} ответов выбрано`);
    }

    // Обработчик клавиш
    function handleKeyPress(event) {
        if (event.key === '2' || event.keyCode === 50) {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
                selectNextAnswer();
            }
        }

        // Клавиша R для сброса
        if (event.key === 'r' || event.key === 'R') {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                currentQuestionAnswers = null;
                currentAnswerIndex = 0;
                selectedAnswersCount = 0;
                console.log('🔄 Состояние сброшено');
            }
        }
    }

    // Инициализация
    function init() {
        document.addEventListener('keydown', handleKeyPress);
        document.addEventListener('keypress', handleKeyPress);
        console.log('🚀 СКРИПТ INTUIT AUTO COMPLETE ЗАПУЩЕН!');
        console.log('📋 Нажмите "2" для выбора следующего правильного ответа');
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();