// ==UserScript==
// @name         Test 7 - Auto Select on Hover
// @namespace    http://tampermonkey.net/
// @version      6.3
// @description  Автоматически выбирает правильные ответы при наведении курсора
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/125
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "QoS это": [
        "определение механизмов качества обслуживания"
    ],
    "Объективными, измеряемыми или рассматриваемыми показателями качества являются": [
        "изменение задержки в сети",
        "пропускная способности сети"
    ],
    "Время отклика оценивается по": [
        "промежутку времени от момента передачи пакета до момента приема подтверждения",
        "пропускной способности",
        "времени задержки однонаправленного сквозного соединения"
    ],
    "Уменьшить задержку вносимую сетью можно за счет": [
        "выделения пакетов содержащих информацию реального времени",
        "улучшения дизайна инфраструктуры"
    ],
    "Готовность и надежность оценивается по": [
        "коэффициенту использования",
        "результатам контроля уровня обслуживания 24/7",
        "возможности получения доступа к ресурсам сети"
    ],
    "Меры обеспечения QoS, применяемые в IP-сетях": [
        "перемаршрутизация",
        "приоритезация трафика",
        "резервирование ресурсов"
    ],
    "К достоинствам модели Diff-Serv можно отнести": [
        "единое понимание метода обработки определенного трафика",
        "отсутствие необходимости резервирования ресурсов"
    ],
    "Общая задержка речевой информации делится на следующие основные части": [
        "задержка кодеков",
        "задержка среды передачи (сети)"
    ],
    "В модели Diff-Serv поступающий в сеть трафик классифицируется и нормализуется": [
        "пограничными маршрутизаторами"
    ],
    "Проблемы с масштабируемостью IntServ возникают в связи с": [
        "зависимостью от количества обрабатываемых потоков"
    ],
    "Механизм обеспечения QoS применяемый в Diff-Serv, включает в себя следующие операции": [
        "маркировка пакетов",
        "классификация пакетов на основании их заголовков",
        "организация очередей с учетом приоритетов"
    ],
    "Отметьте обязательные элементы узла поддерживающего IntServ": [
        "классификатор",
        "диспетчер пакетов",
        "блок управления доступом",
        "протокол резервирования ресурсов"
    ],
    "Для модели IntServ рекомендуется использовать протокол": [
        "RSVP"
    ],
    "Сервисная модель IntServ ориентирована на обслуживание": [
        "единичных потоков"
    ],
    "В случае модели IntServ объем ресурсов, которые необходимы маршрутизатору для обработки и хранения информации RSVP": [
        "увеличивается пропорционально количеству потоков QoS"
    ],
    "Протокол RSVP предназначен для": [
        "резервирования части пропускной способности",
        "гарантирования пропускной способности канала"
    ],
    "Cтандарт RFC2998 описывает": [
        "принципы организации взаимодействия IntServ/RSVP и DiffServ для предоставления QoS из конца в конец"
    ],
    "Согласно RFC2998 возможна организация следующих вариантов взаимодействия протоколов QoS": [
        "выделение ресурсов на статической основе",
        "обработка RSVP-сообщений в DiffServ-регионе"
    ],
    "Резервирование в RSVP производится": [
        "в одном направлении"
    ],
    "RSVP -": [
        "не допускает смешения аудио и видеосигналов на зарезервированном канале"
    ],
    "Использование RSVP сеансов связи позволяет": [
        "автоматически освобождать ресурсы канала при завершении сеансов",
        "в случае невозможности вызова отмены освобождения средства протокола автоматически отменяют запрос на ресурсы"
    ],
    "RSVP для обеспечения QoS имитирует поверх TCP/IP": [
        "коммутацию каналов"
    ],
    "Multiprotocol Label Switching - это": [
        "технология быстрой коммутации пакетов в многопротокольных сетях, основанная на использовании меток"
    ],
    "MPLS позиционируется как": [
        "способ построения высокоскоростных IP-магистралей"
    ],
    "Основные требования, предъявляемые к технологии магистральной сети": [
        "высокая пропускная способность",
        "малое значение задержки"
    ],
    "Укажите корректную последовательность методов обеспечения QoS в технологиях соответственно IntServ, DiffServ, MPLS": [
        "резервирование, приоретизация, перемаршрутизация"
    ],
    "MPLS позволяет": [
        "строить магистральные сети, имеющих практически неограниченные возможности масштабирования",
        "интегрировать сети IP и АТМ",
        "обеспечить высокую гибкость организации дополнительных сервисов"
    ],
    "Преимущества технологии MPLS": [
        "отделение выбора маршрута от анализа IP-адреса",
        "сохранение инвестиций в установленное AТМ-оборудование",
        "ускоренная коммутация"
    ],
    "Алгоритм LLQ служит для": [
        "обеспечения малой задержки"
    ],
    "Вероятность 'потери' пакета конкретного соединения": [
        "прямо пропорциональна пропускной способности, используемой этим соединением"
    ],
    "В MPLS маршрутизаторы ядра занимаются": [
        "коммутацией"
    ],
    "MPLS (многопротокольная коммутация по меткам) предназначена для": [
        "ускорения коммутации пакетов в транспортных сетях"
    ],
    "Разный уровень QoS трафику отдельных классов предоставляется с помощью": [
        "алгоритмов обслуживания очередей"
    ]
};

    let currentQuestionText = null;
    let currentCorrectAnswers = null;
    let hoverEnabled = true;
    let hoverTimer = null;
    let currentHoverElement = null;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска правильных ответов для текущего вопроса
    function findCorrectAnswersForQuestion(questionText) {
        const normalizedQuestion = normalizeText(questionText);

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                return answers;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                return answers;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                return answers;
            }
        }

        return null;
    }

    // Функция для получения текущего вопроса
    function getCurrentQuestion() {
        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            return null;
        }

        return questionElement.textContent.trim();
    }

    // Функция для обновления кэша правильных ответов
    function updateCorrectAnswersCache() {
        const questionText = getCurrentQuestion();

        if (!questionText) {
            currentQuestionText = null;
            currentCorrectAnswers = null;
            return;
        }

        // Если вопрос изменился
        if (questionText !== currentQuestionText) {
            console.log(`📝 Текущий вопрос: "${questionText}"`);
            currentQuestionText = questionText;
            currentCorrectAnswers = findCorrectAnswersForQuestion(questionText);

            if (currentCorrectAnswers) {
                console.log(`✅ Найдены правильные ответы (${currentCorrectAnswers.length}):`, currentCorrectAnswers);
            } else {
                console.log('❌ Правильные ответы не найдены');
            }
        }
    }

    // Функция для проверки, является ли вариант ответа правильным
    function isOptionCorrect(optionElement) {
        if (!currentCorrectAnswers || !hoverEnabled) return false;

        const rightSpan = optionElement.querySelector('.right');
        const optionText = rightSpan ? rightSpan.textContent : optionElement.textContent;

        return currentCorrectAnswers.some(correctAnswer =>
            textsMatch(optionText, correctAnswer)
        );
    }

    // Функция для выбора варианта ответа
    function selectOption(optionElement) {
        if (!hoverEnabled) return;

        const input = optionElement.querySelector('input[type="radio"], input[type="checkbox"]');
        if (!input) return;

        if (!input.checked) {
            input.checked = true;
            input.dispatchEvent(new Event('change', { bubbles: true }));
            input.dispatchEvent(new Event('click', { bubbles: true }));

            console.log(`✅ Автоматически выбран: "${optionElement.textContent.trim()}"`);
        }
    }

    // Обработчик наведения мыши
    function handleMouseOver(event) {
        updateCorrectAnswersCache();

        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Проверяем, правильный ли это ответ
        if (isOptionCorrect(target)) {
            currentHoverElement = target;

            // Запускаем таймер с задержкой 500ms
            hoverTimer = setTimeout(() => {
                if (currentHoverElement === target) {
                    selectOption(target);
                    currentHoverElement = null;
                }
            }, 500); // 0.5 секунды задержки
        }
    }

    // Обработчик ухода мыши с элемента
    function handleMouseOut(event) {
        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Если ушли с элемента, который был наведен, отменяем таймер
        if (target === currentHoverElement) {
            clearTimeout(hoverTimer);
            hoverTimer = null;
            currentHoverElement = null;
        }
    }

    // Обработчик клавиш для отладки
    function handleKeyPress(event) {
        // Ctrl+Shift+D для вывода отладочной информации
        if (event.ctrlKey && event.shiftKey && event.key === 'D') {
            console.log('=== ОТЛАДОЧНАЯ ИНФОРМАЦИЯ ===');
            console.log('Текущий вопрос:', currentQuestionText);
            console.log('Правильные ответы:', currentCorrectAnswers);
            console.log('Автовыбор включен:', hoverEnabled);
        }

        // Ctrl+Shift+T для переключения режима
        if (event.ctrlKey && event.shiftKey && event.key === 'T') {
            hoverEnabled = !hoverEnabled;
            console.log(hoverEnabled ? '✅ Автовыбор включен' : '❌ Автовыбор выключен');
        }
    }

    // Инициализация
    function init() {
        // Добавляем обработчик наведения мыши
        document.addEventListener('mouseover', handleMouseOver);
        document.addEventListener('mouseout', handleMouseOut);

        // Добавляем обработчик клавиш для отладки
        document.addEventListener('keydown', handleKeyPress);

        // Обновляем кэш при клике (для перехода к следующему вопросу)
        document.addEventListener('click', updateCorrectAnswersCache);

        console.log('🚀 СКРИПТ АВТОВЫБОРА ЗАПУЩЕН!');
        console.log('🎯 Наведите курсор на правильный ответ для автоматического выбора');
        console.log('⏱️  Автовыбор произойдет через 0.5 секунды после наведения');
        console.log('🔄 Нажмите Ctrl+Shift+T для переключения режима автовыбора');
        console.log('📊 Нажмите Ctrl+Shift+D для отладочной информации');
    }

    // Запуск при загрузке страницы
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();
