// ==UserScript==
// @name         Test 10 - Auto Select on Hover
// @namespace    http://tampermonkey.net/
// @version      6.3
// @description  Автоматически выбирает правильные ответы при наведении курсора
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/128
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "Биллинг IP-услуг включает": [
        "систему учета потребленных услуг",
        "систему тарификации потребленных услуг"
    ],
    "Биллинг - это": [
        "система выставления счетов за потребленные услуги"
    ],
    "Системы тарификации потребленных услуг включают модули": [
        "аутентификации",
        "биллинга",
        "авторизации"
    ],
    "IP услуги в сетях определяются": [
        "метриками"
    ],
    "В коммутируемых сетях учетные данные состоят в основном из": [
        "событий"
    ],
    "Условиями для выставления счета являются": [
        "наличие предоставленного пакета услуг",
        "ведение учета в режиме реального времени",
        "ведение учетной информации"
    ],
    "Качество обслуживания, в IP-сетях": [
        "является изменяемым параметром"
    ],
    "Учетные данные событий в коммутируемых сетях состоят из следующих данных": [
        "начало сеанса связи",
        "цифра номера",
        "завершение сеанса связи",
        "использование дополнительных услуг"
    ],
    "Метриками в сетях IP услуг являются": [
        "количество переданных и принятых байтов",
        "число пакетов",
        "номера портов",
        "адреса IP"
    ],
    "В рамках предоставления сервисов операторы IP": [
        "используют несколько различных сетей и служб"
    ],
    "Набор метрик зависит от": [
        "типа предоставляемых услуг"
    ],
    "Обработку учетной информации и биллинг в реальном масштабе времени необходимо проводить с целью": [
        "прекратить предоставление услуги абоненту при истечении средств на его счету",
        "предоставления услуг абоненту в четком соответствии со средствами на его счету",
        "предоставления абоненту реальной информации о состоянии средств на его счету"
    ],
    "Данные системы биллинга используются для": [
        "оптимизации конфигурации сети",
        "планирования емкости хранилищ",
        "финансовых взаиморасчетов"
    ],
    "Какие индустриальные стандарты существуют в области биллинга услуг?": [
        "стандартов не существует"
    ],
    "При традиционной телефонной связи программное обеспечение, которое выполняет расчеты за пользование услугами телефонной связи": [
        "обобщает информацию в конце расчетного периода"
    ],
    "Детальная информация о вызовах содержится в": [
        "CDR"
    ],
    "Система биллинга и менеджмента пользователей создает комплексную архитектуру реального времени, которая обеспечивает:": [
        "поиск наилучшего пути для вызова",
        "множественный доступ при одном предоплаченном счете",
        "получение информации о зональных тарифах, остатках на счетах и другой информации по обработке и маршрутизированию заранее оплаченных вызовов в реальном времени"
    ],
    "Система менеджмента и биллинга, используемая для поддержки услуг IP-телефонии": [
        "функционирует в режиме реального времени",
        "обеспечивает быстрое развертывание новых услуг",
        "позволяет пользователям следить за состоянием своего счета"
    ],
    "Биллинг услуг IP-телефонии осуществляется": [
        "в режиме реального времени"
    ],
    "Расчеты с абонентами подразумевают": [
        "поддержку различных периодов оплаты счетов",
        "поддержка расчетов в нескольких валютах"
    ],
    "Первичные провайдеры предлагают своим вторичным провайдерам услуги": [
        "тарификации",
        "менеджмент пользователя",
        "создания счетов"
    ],
    "Система биллинга и менеджмента пользователей IP-телефонии поддерживают": [
        "уникальную аутентификацию пользователей"
    ],
    "Субпровайдеры -": [
        "предлагают продукты или услуги интернет-телефонии под их собственной корпоративной маркой без необходимости создания инфраструктуры менеджмента и поддержки услуг первичной сети"
    ],
    "Традиционные биллинг услуги ТФоП": [
        "не включают процесс авторизации пользователей"
    ],
    "Система IAF Horizon обеспечивает биллинг использования инфраструктуры построенной на базе сетевого оборудования фирм": [
        "Cisco",
        "Ericsson"
    ],
    "Система IPhonEX поддерживает следующие формы расчетов с пользователями": [
        "неограниченный кредит",
        "ограниченный кредит",
        "дебетовая плата/телефонные карточки"
    ],
    "Укажите возможные варианты расширения списка услуг провайдера IP-телефонии": [
        "Dial-Up",
        "E-Mail account",
        "Web-hosting"
    ],
    "Отметьте одни из наиболее распространенных систем биллинга": [
        "IMS 3.x",
        "iPhoneEX"
    ],
    "Особенности системы Intranet IPT": [
        "работа с кредитными карточками",
        "возможность выбора маршрута наименьшей стоимости",
        "междоменное согласование и регулирование тарифов"
    ],
    "Дополнительно к традиционным услугам тарификации IP-телефонии IAF Horizon обеспечивает": [
        "поддержку управления вызовом во время соединения",
        "использование сообщений интерактивной системы ответа IVR",
        "перенаправление вызова и блокирование номера"
    ],
    "Дополнительно к базовым возможности система Talking NT обеспечивает": [
        "международный обратный вызов",
        "заранее оплаченный беспроводный доступ",
        "услуга персонального номера"
    ],
    "Особенности работы системы Intranet IPT в реальном масштабе времени": [
        "возможность выбора маршрута наименьшей стоимости",
        "междоменное согласование и регулирование тарифов"
    ],
    "Модуль Traffic системы IPhonEX": [
        "является инструментом для анализа загрузок шлюза"
    ]
};


    let currentQuestionText = null;
    let currentCorrectAnswers = null;
    let hoverEnabled = true;
    let hoverTimer = null;
    let currentHoverElement = null;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска правильных ответов для текущего вопроса
    function findCorrectAnswersForQuestion(questionText) {
        const normalizedQuestion = normalizeText(questionText);

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                return answers;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                return answers;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                return answers;
            }
        }

        return null;
    }

    // Функция для получения текущего вопроса
    function getCurrentQuestion() {
        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            return null;
        }

        return questionElement.textContent.trim();
    }

    // Функция для обновления кэша правильных ответов
    function updateCorrectAnswersCache() {
        const questionText = getCurrentQuestion();

        if (!questionText) {
            currentQuestionText = null;
            currentCorrectAnswers = null;
            return;
        }

        // Если вопрос изменился
        if (questionText !== currentQuestionText) {
            console.log(`📝 Текущий вопрос: "${questionText}"`);
            currentQuestionText = questionText;
            currentCorrectAnswers = findCorrectAnswersForQuestion(questionText);

            if (currentCorrectAnswers) {
                console.log(`✅ Найдены правильные ответы (${currentCorrectAnswers.length}):`, currentCorrectAnswers);
            } else {
                console.log('❌ Правильные ответы не найдены');
            }
        }
    }

    // Функция для проверки, является ли вариант ответа правильным
    function isOptionCorrect(optionElement) {
        if (!currentCorrectAnswers || !hoverEnabled) return false;

        const rightSpan = optionElement.querySelector('.right');
        const optionText = rightSpan ? rightSpan.textContent : optionElement.textContent;

        return currentCorrectAnswers.some(correctAnswer =>
            textsMatch(optionText, correctAnswer)
        );
    }

    // Функция для выбора варианта ответа
    function selectOption(optionElement) {
        if (!hoverEnabled) return;

        const input = optionElement.querySelector('input[type="radio"], input[type="checkbox"]');
        if (!input) return;

        if (!input.checked) {
            input.checked = true;
            input.dispatchEvent(new Event('change', { bubbles: true }));
            input.dispatchEvent(new Event('click', { bubbles: true }));

            console.log(`✅ Автоматически выбран: "${optionElement.textContent.trim()}"`);
        }
    }

    // Обработчик наведения мыши
    function handleMouseOver(event) {
        updateCorrectAnswersCache();

        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Проверяем, правильный ли это ответ
        if (isOptionCorrect(target)) {
            currentHoverElement = target;

            // Запускаем таймер с задержкой 500ms
            hoverTimer = setTimeout(() => {
                if (currentHoverElement === target) {
                    selectOption(target);
                    currentHoverElement = null;
                }
            }, 500); // 0.5 секунды задержки
        }
    }

    // Обработчик ухода мыши с элемента
    function handleMouseOut(event) {
        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Если ушли с элемента, который был наведен, отменяем таймер
        if (target === currentHoverElement) {
            clearTimeout(hoverTimer);
            hoverTimer = null;
            currentHoverElement = null;
        }
    }

    // Обработчик клавиш для отладки
    function handleKeyPress(event) {
        // Ctrl+Shift+D для вывода отладочной информации
        if (event.ctrlKey && event.shiftKey && event.key === 'D') {
            console.log('=== ОТЛАДОЧНАЯ ИНФОРМАЦИЯ ===');
            console.log('Текущий вопрос:', currentQuestionText);
            console.log('Правильные ответы:', currentCorrectAnswers);
            console.log('Автовыбор включен:', hoverEnabled);
        }

        // Ctrl+Shift+T для переключения режима
        if (event.ctrlKey && event.shiftKey && event.key === 'T') {
            hoverEnabled = !hoverEnabled;
            console.log(hoverEnabled ? '✅ Автовыбор включен' : '❌ Автовыбор выключен');
        }
    }

    // Инициализация
    function init() {
        // Добавляем обработчик наведения мыши
        document.addEventListener('mouseover', handleMouseOver);
        document.addEventListener('mouseout', handleMouseOut);

        // Добавляем обработчик клавиш для отладки
        document.addEventListener('keydown', handleKeyPress);

        // Обновляем кэш при клике (для перехода к следующему вопросу)
        document.addEventListener('click', updateCorrectAnswersCache);

        console.log('🚀 СКРИПТ АВТОВЫБОРА ЗАПУЩЕН!');
        console.log('🎯 Наведите курсор на правильный ответ для автоматического выбора');
        console.log('⏱️  Автовыбор произойдет через 0.5 секунды после наведения');
        console.log('🔄 Нажмите Ctrl+Shift+T для переключения режима автовыбора');
        console.log('📊 Нажмите Ctrl+Shift+D для отладочной информации');
    }

    // Запуск при загрузке страницы
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();
