// ==UserScript==
// @name         Test 5
// @namespace    http://tampermonkey.net/
// @version      6.0
// @description  Автоматически выбирает все правильные ответы и нажимает "Ответить"
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/123
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "SIP это": [
        "Session Initiation Protocol"
    ],
    "Протокол SIP в стеке протоколов TCP/IP находится на": [
        "прикладном уровне"
    ],
    "Отметьте принципы заложенные в основу протокола SIP": [
        "интеграция в стек существующих протоколов Интернета",
        "масштабируемость сети",
        "взаимодействие с другими протоколами сигнализации",
        "расширяемость протокола"
    ],
    "Провайдер расширил функционал своих услуг за счет введения нового но еще незарегистрированного в IANA функционала. Смогут ли внешние пользователи обращаясь к внутренним пользователям провайдера воспользоваться этим дополнительным функционалом?": [
        "нет",
        "да, после регистрации в IANA"
    ],
    "В отличие от терминалов, описанных в рекомендациях Н.323, терминалы SIP выполняют функции": [
        "шлюза"
    ],
    "Сервер SIP, после принятия запроса, обращается к серверу местоположения для того, чтобы узнать адрес, по которому можно найти пользователя. Сервер местоположения": [
        "предоставляет информацию о месторасположении пользователя при наличии таковой",
        "может предоставить информацию о невозможности найти пользователя"
    ],
    "Компания использует конференции для повседневной работы. Какой протокол предпочтительнее использовать если известно что вся сеть для создания интранета использует туннелирование, сетевые экраны используются только периметру а качество сети близко к идеальному (отсутствуют потерянные пакеты)": [
        "UDP"
    ],
    "Протокол описания сеансов связи используется для": [
        "обмена между сторонами данными о функциональных возможностях сторон"
    ],
    "Протокол SIP предусматривает организацию конференций следующих видов": [
        "в режиме многоадресной рассылки (multicasting)",
        "при помощи контроллера управления конференции (MCU)",
        "путем соединения каждого пользователя с каждым в режиме 'точка-точка'"
    ],
    "С помощью какого протокола реализуется возможность превращения телефонного разговора в видео-звонок не прерывая сеанс связи?": [
        "SDP"
    ],
    "Возможные типы прокси-серверов": [
        "с сохранением состояний",
        "без сохранения состояний"
    ],
    "В качестве основной компоненты протокола SIP прокси сервер": [
        "принимает и обрабатывает запросы",
        "инициирует запросы"
    ],
    "Использование какого протокола позволит преобразовать телефонный разговор в конференцию с числом участников более 2-х": [
        "H.323",
        "SIP"
    ],
    "Прокси-сервер может функционировать": [
        "как сервер с сохранением состояний для одних пользователей и как сервер без сохранения состояний - для других"
    ],
    "База данных адресной информации хранится в": [
        "сервере определения местоположения пользователей"
    ],
    "Отметьте корректные записи SIP-адреса:": [
        "sip: als@rts.loniis.ru",
        "имя@хост",
        "имя@IР-адрес",
        "sip: 294-75-47@gateway.ru"
    ],
    "В сети SIP с прокси-сервером успешное установление двустороннего соединения между инициирующей и принимающей сторонами осуществляется за ____ шагов": [
        "6"
    ],
    "В сети SIP с сервером переадресации успешное установление двустороннего соединения между инициирующей и принимающей сторонами осуществляется за ____ шагов": [
        "5"
    ],
    "Прокси-сервера типа stateful": [
        "предоставляют большее количество услуг чем сервер типа stateless"
    ],
    "Процедура ACK служат для": [
        "подтверждения получения сообщения"
    ],
    "Процедура BYE": [
        "завершает соединение между двумя пользователями"
    ],
    "Сервер переадресации предназначен для определения": [
        "текущего адреса"
    ],
    "Процедура CANCEL": [
        "используется для передачи информации о поддерживаемых характеристиках"
    ],
    "Процедура OPTIONS": [
        "используется для передачи информации о поддерживаемых характеристиках"
    ],
    "Сколько видов сигнальных сообщений определены в протоколе SIP?": [
        "2"
    ],
    "Процедура REGISTER": [
        "передает информацию о местоположении пользователя на сервер SIP, который может транслировать ее на сервер адресов"
    ],
    "SIP терминалы могут представлять из себя": [
        "SIP-телефон, подключающийся непосредственно к ЛВС",
        "персональный компьютер со звуковой платой и программным обеспечением SIP-клиента"
    ],
    "Сколько процедур определено в протоколе SIP?": [
        "6"
    ],
    "Обработка пользователей на сервере SIP телефонии может осуществляться": [
        "по разным правилам"
    ],
    "Сети SIP обычно строятся из элементов трех основных типов. Укажите корректные элементы": [
        "терминал",
        "прокси-сервер",
        "сервер-переадресации"
    ],
    "Принцип работы сервера месторасположения": [
        "не определяется рекомендацией SIP"
    ],
    "Чем отличается соединение SIP с сервером переадресации от соединения SIP с прокси-сервером": [
        "сервер переадресации не выдает INVITE"
    ],
    "Отметьте корректные утверждения": [
        "все запросы хранятся в памяти сервера statefull только до окончания транзакции",
        "прокси-сервер должен сохранять информацию о состояниях, если он использует протокол TCP для передачи сигнальной информации"
    ]
};

    let currentAnswerIndex = 0;
    let currentQuestionAnswers = null;
    let selectedAnswersCount = 0;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска всех подходящих вопросов
    function findAllMatchingQuestions(questionText) {
        const normalizedQuestion = normalizeText(questionText);
        let matchingQuestions = [];

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                matchingQuestions.push({ question, answers, type: 'exact', priority: 1 });
                continue;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                matchingQuestions.push({ question, answers, type: 'contains', priority: 2 });
                continue;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                matchingQuestions.push({ question, answers, type: 'partial', priority: 3, percentage: matchPercentage });
            }
        }

        // Сортируем по приоритету
        matchingQuestions.sort((a, b) => a.priority - b.priority);

        return matchingQuestions;
    }

    // Функция для выбора одного ответа по очереди
    function selectNextAnswer() {
        console.log('🎯 ВЫБОР СЛЕДУЮЩЕГО ОТВЕТА');

        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            console.log('❌ Элемент вопроса не найден');
            return;
        }

        const questionText = questionElement.textContent.trim();

        // Если это новый вопрос, найдем все правильные ответы
        if (!currentQuestionAnswers) {
            console.log(`📝 НОВЫЙ ВОПРОС: "${questionText}"`);

            const matchingQuestions = findAllMatchingQuestions(questionText);

            if (matchingQuestions.length === 0) {
                console.log('❌ Подходящие вопросы не найдены');
                return;
            }

            // Берем первый подходящий вопрос
            currentQuestionAnswers = matchingQuestions[0].answers;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;

            console.log(`✅ ПРАВИЛЬНЫЕ ОТВЕТЫ (${currentQuestionAnswers.length}):`, currentQuestionAnswers);
        }

        // Если все ответы уже выбраны
        if (currentAnswerIndex >= currentQuestionAnswers.length) {
            console.log('✅ ВСЕ ОТВЕТЫ ВЫБРАНЫ! Можете нажать "Ответить" вручную.');
            // Сбрасываем для следующего вопроса
            currentQuestionAnswers = null;
            currentAnswerIndex = 0;
            selectedAnswersCount = 0;
            return;
        }

        const targetAnswer = currentQuestionAnswers[currentAnswerIndex];
        console.log(`🔍 ИЩЕМ ОТВЕТ ${currentAnswerIndex + 1}/${currentQuestionAnswers.length}: "${targetAnswer}"`);

        let answerOptions = document.querySelectorAll('.wrapper label.option');
        if (answerOptions.length === 0) {
            answerOptions = document.querySelectorAll('label.option');
        }

        if (answerOptions.length === 0) {
            console.log('❌ Варианты ответов не найдены');
            return;
        }

        let found = false;

        answerOptions.forEach((option, index) => {
            if (found) return;

            const rightSpan = option.querySelector('.right');
            const optionText = rightSpan ? rightSpan.textContent : option.textContent;

            console.log(`   Проверяем вариант ${index + 1}: "${optionText.trim()}"`);

            if (textsMatch(optionText, targetAnswer)) {
                let input = option.querySelector('input[type="radio"], input[type="checkbox"]');
                if (input && !input.checked) {
                    input.checked = true;
                    input.dispatchEvent(new Event('change', { bubbles: true }));
                    input.dispatchEvent(new Event('click', { bubbles: true }));
                    console.log(`✅ ВЫБРАН вариант ${index + 1}: "${optionText.trim()}"`);
                    found = true;
                    selectedAnswersCount++;
                    currentAnswerIndex++;
                } else if (input && input.checked) {
                    console.log(`⚠️ Вариант ${index + 1} уже выбран, переходим к следующему`);
                    found = true;
                    currentAnswerIndex++;
                }
            }
        });

        if (!found) {
            console.log(`❌ Ответ "${targetAnswer}" не найден, переходим к следующему`);
            currentAnswerIndex++;

            // Если не нашли текущий ответ, пробуем следующий
            if (currentAnswerIndex < currentQuestionAnswers.length) {
                setTimeout(() => selectNextAnswer(), 500);
            }
        }

        console.log(`📊 ПРОГРЕСС: ${selectedAnswersCount}/${currentQuestionAnswers.length} ответов выбрано`);
    }

    // Обработчик клавиш
    function handleKeyPress(event) {
        if (event.key === '2' || event.keyCode === 50) {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                event.preventDefault();
                selectNextAnswer();
            }
        }

        // Клавиша R для сброса
        if (event.key === 'r' || event.key === 'R') {
            if (event.target.tagName !== 'INPUT' && event.target.tagName !== 'TEXTAREA') {
                currentQuestionAnswers = null;
                currentAnswerIndex = 0;
                selectedAnswersCount = 0;
                console.log('🔄 Состояние сброшено');
            }
        }
    }

    // Инициализация
    function init() {
        document.addEventListener('keydown', handleKeyPress);
        document.addEventListener('keypress', handleKeyPress);
        console.log('🚀 СКРИПТ INTUIT AUTO COMPLETE ЗАПУЩЕН!');
        console.log('📋 Нажмите "2" для выбора следующего правильного ответа');
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();