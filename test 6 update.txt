// ==UserScript==
// @name         Test 6 - Auto Select on Hover
// @namespace    http://tampermonkey.net/
// @version      6.3
// @description  Автоматически выбирает правильные ответы при наведении курсора
// @author       You
// @match        https://intuit.ru/studies/courses/8/8/test/1/124
// @grant        none
// @run-at       document-start
// ==/UserScript==

(function() {
    'use strict';

    // База данных с правильными ответами по тексту
    const correctAnswerTexts = {
    "Принцип декомпозиции шлюза подразумевает": [
        "разбиение функционала шлюза на блоки"
    ],
    "Принцип построения MGCP состоит в": [
        "отделении управляющей сигнализации от медиа-потоков",
        "том что управление сигнализацией сосредоточено на центральном управляющем устройстве"
    ],
    "В архитектуре протокола MGCP-сети устройством управления вызовами может называться": [
        "контроллер сигнализаций",
        "контроллер шлюза",
        "программный контроллер"
    ],
    "Архитектура протокола MGCP-сети включает следующие основные функциональные компоненты": [
        "MG",
        "SS",
        "MGC"
    ],
    "Контроллер сигнализации обеспечивает": [
        "функции управления шлюзами",
        "обмен сигнальной информацией",
        "согласование между традиционной телефонной сетью и сетью IP"
    ],
    "Транспортный шлюз (Media Gateway) выполняет функции": [
        "кодирование и упаковку речевой информации в пакеты RTP/UDP/IP",
        "преобразования речевой информации, поступающей со стороны ТфОП"
    ],
    "Декомпозиция шлюза осуществляется на следующие блоки": [
        "Media Gateway",
        "Call Agent",
        "Signaling Gateway"
    ],
    "Устройство управления вызовами (Call Agent) выполняет следующие функции": [
        "управление шлюзом"
    ],
    "Шлюз сигнализации (Signaling Gateway) обеспечивает": [
        "перенос сигнальной информации в обратном направлении",
        "доставку сигнальной информации поступающей со стороны ТфОП, к устройству управления шлюзом"
    ],
    "Специализированные шлюзы получаются путем": [
        "объединения набора определенных команд"
    ],
    "Какой из шлюзов обеспечивает доставку сигнальной информации, поступающей со стороны ТфОП, к устройству управления шлюзом": [
        "Signaling Gateway"
    ],
    "Какой из шлюзов выполняет функции преобразования речевой информации, поступающей со стороны ТфОП, в вид пригодный для передачи по сетям с маршрутизацией пакетов IP": [
        "Media Gateway"
    ],
    "Контроллер сигнализаций CA управляет элементами с помощью": [
        "ASCII сообщений"
    ],
    "Контроллеры обмениваются со шлюзами (или IP-телефонами) аудиоданными в": [
        "текстовом формате"
    ],
    "Функциональное назначение каждого шлюза определяется": [
        "набором команд, которые он 'понимает'"
    ],
    "Контроллер сигнализаций CA обрабатывает события для физических и виртуальных элементов": [
        "одним и тем же способом"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Business Gateway представляет из себя": [
        "шлюз с цифровым интерфейсом для подключения к сети с маршрутизацией IP-пакетов учрежденческой АТС"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Access Gateway представляет из себя": [
        "шлюз для подключения к сети IP-телефонии небольшой учрежденческой АТС через аналоговый или цифровой интерфейс"
    ],
    "Укажите шлюзы Media Gateways в списке:": [
        "packet switch",
        "access gateway",
        "Network Access Server"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Residential Gateway представляет из себя": [
        "шлюз, подключающий к IP-сети аналоговые, кабельные модемы, линии xDSL и широкополосные устройства беспроводного доступа"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Trunking Gateway представляет из себя": [
        "шлюз между ТфОП и сетью с маршрутизацией пакетов IP, ориентированный на подключение к телефонной сети"
    ],
    "Базовыми компонентами процесса обслуживания вызова с использованием протокола MGCP являются": [
        "Endpoints",
        "Connections"
    ],
    "Примером виртуального порта является": [
        "программа-бот синтезирующая голосовые сообщения",
        "источник речевой информации в интерактивном речевом сервере"
    ],
    "Укажите физическую реализацию ENDPOINT's": [
        "аналоговые интерфейсы, поддерживающие каждый одно телефонное соединение",
        "цифровые каналы, поддерживающие одно телефонное соединение"
    ],
    "Endpoints - это": [
        "порты оборудования, являющиеся источниками и приемниками информации"
    ],
    "Connection означает": [
        "подключение порта к одному из двух концов соединения, которое создается между ним и другим портом"
    ],
    "С помощью какой команды устройство управления инструктирует шлюз, каким образом он должен обрабатывать получаемые речевые сигналы?": [
        "EndpointConfiguration"
    ],
    "При помощи протокола MGCP устройство управления и шлюз обмениваются командами представляющими из себя": [
        "набор текстовых строк"
    ],
    "При помощи какой команды можно дать указание шлюзу выявлять определенные события или сигналы и информировать о них устройство управления?": [
        "Notification Request"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Voice over ATM Gateway представляет из себя": [
        "шлюз между ТфОП и АТМ-сетью, который также подключается к телефонной сети посредством большого количества цифровых трактов"
    ],
    "С помощью Call Agent можно определить": [
        "поднятие трубки абонента",
        "DMTF"
    ],
    "Среди протоколов H.323, SIP, MGCP наименее используемым является": [
        "MGCP"
    ],
    "В классификации транспортных шлюзов (Media Gateways) - Network Access Server представляет из себя": [
        "сервер доступа к IP-сети для передачи данных"
    ]
};

    let currentQuestionText = null;
    let currentCorrectAnswers = null;
    let hoverEnabled = true;
    let hoverTimer = null;
    let currentHoverElement = null;

    // Функция для нормализации текста
    function normalizeText(text) {
        return text.trim().toLowerCase()
            .replace(/[.,!?;:()]/g, '')
            .replace(/\s+/g, ' ')
            .trim();
    }

    // Функция для точного сравнения текстов
    function textsMatch(optionText, correctText) {
        const normalizedOption = normalizeText(optionText);
        const normalizedCorrect = normalizeText(correctText);

        if (!normalizedOption || !normalizedCorrect) return false;

        return normalizedOption === normalizedCorrect;
    }

    // Функция для поиска правильных ответов для текущего вопроса
    function findCorrectAnswersForQuestion(questionText) {
        const normalizedQuestion = normalizeText(questionText);

        for (const [question, answers] of Object.entries(correctAnswerTexts)) {
            const normalizedDbQuestion = normalizeText(question);

            // Точное совпадение
            if (normalizedQuestion === normalizedDbQuestion) {
                return answers;
            }

            // Содержание
            if (normalizedQuestion.includes(normalizedDbQuestion) || normalizedDbQuestion.includes(normalizedQuestion)) {
                return answers;
            }

            // Частичное совпадение
            const questionWords = normalizedQuestion.split(' ').filter(w => w.length > 3);
            const dbWords = normalizedDbQuestion.split(' ').filter(w => w.length > 3);
            const matchingWords = questionWords.filter(w => dbWords.includes(w));
            const matchPercentage = matchingWords.length / Math.min(questionWords.length, dbWords.length);

            if (matchPercentage >= 0.8) {
                return answers;
            }
        }

        return null;
    }

    // Функция для получения текущего вопроса
    function getCurrentQuestion() {
        let questionElement = document.querySelector('.question');
        if (!questionElement) {
            questionElement = document.querySelector('.task_title');
        }

        if (!questionElement) {
            return null;
        }

        return questionElement.textContent.trim();
    }

    // Функция для обновления кэша правильных ответов
    function updateCorrectAnswersCache() {
        const questionText = getCurrentQuestion();

        if (!questionText) {
            currentQuestionText = null;
            currentCorrectAnswers = null;
            return;
        }

        // Если вопрос изменился
        if (questionText !== currentQuestionText) {
            console.log(`📝 Текущий вопрос: "${questionText}"`);
            currentQuestionText = questionText;
            currentCorrectAnswers = findCorrectAnswersForQuestion(questionText);

            if (currentCorrectAnswers) {
                console.log(`✅ Найдены правильные ответы (${currentCorrectAnswers.length}):`, currentCorrectAnswers);
            } else {
                console.log('❌ Правильные ответы не найдены');
            }
        }
    }

    // Функция для проверки, является ли вариант ответа правильным
    function isOptionCorrect(optionElement) {
        if (!currentCorrectAnswers || !hoverEnabled) return false;

        const rightSpan = optionElement.querySelector('.right');
        const optionText = rightSpan ? rightSpan.textContent : optionElement.textContent;

        return currentCorrectAnswers.some(correctAnswer =>
            textsMatch(optionText, correctAnswer)
        );
    }

    // Функция для выбора варианта ответа
    function selectOption(optionElement) {
        if (!hoverEnabled) return;

        const input = optionElement.querySelector('input[type="radio"], input[type="checkbox"]');
        if (!input) return;

        if (!input.checked) {
            input.checked = true;
            input.dispatchEvent(new Event('change', { bubbles: true }));
            input.dispatchEvent(new Event('click', { bubbles: true }));

            console.log(`✅ Автоматически выбран: "${optionElement.textContent.trim()}"`);
        }
    }

    // Обработчик наведения мыши
    function handleMouseOver(event) {
        updateCorrectAnswersCache();

        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Проверяем, правильный ли это ответ
        if (isOptionCorrect(target)) {
            currentHoverElement = target;

            // Запускаем таймер с задержкой 500ms
            hoverTimer = setTimeout(() => {
                if (currentHoverElement === target) {
                    selectOption(target);
                    currentHoverElement = null;
                }
            }, 500); // 0.5 секунды задержки
        }
    }

    // Обработчик ухода мыши с элемента
    function handleMouseOut(event) {
        let target = event.target;

        // Ищем родительский элемент с классом option
        while (target && !target.classList?.contains('option')) {
            target = target.parentElement;
            if (!target || target === document.body) return;
        }

        if (!target || !target.classList.contains('option')) return;

        // Если ушли с элемента, который был наведен, отменяем таймер
        if (target === currentHoverElement) {
            clearTimeout(hoverTimer);
            hoverTimer = null;
            currentHoverElement = null;
        }
    }

    // Обработчик клавиш для отладки
    function handleKeyPress(event) {
        // Ctrl+Shift+D для вывода отладочной информации
        if (event.ctrlKey && event.shiftKey && event.key === 'D') {
            console.log('=== ОТЛАДОЧНАЯ ИНФОРМАЦИЯ ===');
            console.log('Текущий вопрос:', currentQuestionText);
            console.log('Правильные ответы:', currentCorrectAnswers);
            console.log('Автовыбор включен:', hoverEnabled);
        }

        // Ctrl+Shift+T для переключения режима
        if (event.ctrlKey && event.shiftKey && event.key === 'T') {
            hoverEnabled = !hoverEnabled;
            console.log(hoverEnabled ? '✅ Автовыбор включен' : '❌ Автовыбор выключен');
        }
    }

    // Инициализация
    function init() {
        // Добавляем обработчик наведения мыши
        document.addEventListener('mouseover', handleMouseOver);
        document.addEventListener('mouseout', handleMouseOut);

        // Добавляем обработчик клавиш для отладки
        document.addEventListener('keydown', handleKeyPress);

        // Обновляем кэш при клике (для перехода к следующему вопросу)
        document.addEventListener('click', updateCorrectAnswersCache);

        console.log('🚀 СКРИПТ АВТОВЫБОРА ЗАПУЩЕН!');
        console.log('🎯 Наведите курсор на правильный ответ для автоматического выбора');
        console.log('⏱️  Автовыбор произойдет через 0.5 секунды после наведения');
        console.log('🔄 Нажмите Ctrl+Shift+T для переключения режима автовыбора');
        console.log('📊 Нажмите Ctrl+Shift+D для отладочной информации');
    }

    // Запуск при загрузке страницы
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

})();
